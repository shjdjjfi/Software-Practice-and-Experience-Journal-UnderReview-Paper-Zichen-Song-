\settings
{
    "Choice": {
        "intRules": "intRules:rustSemantics", // We enable overflow checks
    }
}

\programVariables {
    x: i32;
}

// Note that we have no more precondition of `x = 0`; hence, x may be i32::MAX and overflow
\problem {
    \<{x = x + 1;}\>(x > 0)
}